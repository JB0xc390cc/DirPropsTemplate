<Project>
    <PropertyGroup>
        <GeneratePackageOnBuild Condition="'$(IsPackeable)' == 'true'">true</GeneratePackageOnBuild>
    </PropertyGroup>
    <ItemGroup>
        <PackageReference Condition="'$(IsPackeable)' == 'true'" Include="DotNet.ReproducibleBuilds"/>
    </ItemGroup>
</Project>